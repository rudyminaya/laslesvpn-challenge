export type MenuItemsTypes = {
    title: string;
    url: string;
}

export type MetricItemTypes = {
    icon: React.ReactNode;
    quantity: number;
    description: string;
}

export type PlanItemTypes = {
    title: string;
    price: number;
    features: string[];
}